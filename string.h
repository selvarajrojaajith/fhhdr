#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
int main ( int argc , char* argv[])
{
char* s = "CPROGRAMMING";
int i;
for(i=0; , i< strlen; i++ )
{
   if( isalpha(s[i]) && isupper(s[i]) )
     flag = true;
   else
     flag = false;
}
if (flag)
     printf("A string contains and alphabet letters");
    
return 0;
}
